unbind C-b
set -g prefix C-s
set -g focus-events on

# Starting the Index with 1
set -g base-index 1
setw -g pane-base-index 1

# Use vim keybindings in copy mode
setw -g mode-keys vi

# Setup 'v' to begin selection as in Vim
bind-key -t vi-copy v begin-selection
bind-key -t vi-copy y copy-pipe "reattach-to-user-namespace pbcopy"

# Update default binding of `Enter` to also use copy-pipe
unbind -t vi-copy Enter
bind-key -t vi-copy Enter copy-pipe "reattach-to-user-namespace pbcopy"

bind C-k send-keys -R \; clear-history
set-option -g default-command "reattach-to-user-namespace -l zsh"

set -g focus-events on
set -s escape-time 0
set -g mouse on
bind-key r source-file ~/.tmux.conf \; display-message "~/.tmux.conf reloaded"

bind v split-window -h -c '#{pane_current_path}'
bind j split-window -v -c '#{pane_current_path}'

set-option -g status-right "#{cpu_icon}  #(battery -t) #(date '+%a, %b %d - %I:%M') "
is_vim="ps -o state= -o comm= -t '#{pane_tty}' \
    | grep -iqE '^[^TXZ ]+ +(\\S+\\/)?g?(view|n?vim?x?)(diff)?$'"
bind-key -n Left if-shell "$is_vim" "send-keys left"  "select-pane -L"
bind-key -n Down if-shell "$is_vim" "send-keys down"  "select-pane -D"
bind-key -n Up if-shell "$is_vim" "send-keys up"  "select-pane -U"
bind-key -n Right if-shell "$is_vim" "send-keys right"  "select-pane -R"
bind-key -n C-\ if-shell "$is_vim" "send-keys C-\\" "select-pane -l"

bind-key J resize-pane -D 10
bind-key K resize-pane -U 10
bind-key H resize-pane -L 20
bind-key L resize-pane -R 20

###########################L#################
# please remember to tmux kill-servertmux kill-servertmux kill-server
# $ tmux kill-servertmux kill-servertmux kill-server


# set-window-option -g aggressive-resize on



set -g -a terminal-overrides ',*:Ss=\E[%p1%d q:Se=\E[2 q'



# Index starts from 1
# set-option -g base-index 1

# select-pane (@george-b)
bind-key o run "tmux split-window -p 90 'zsh -ci fs'"
bind-key 9 run "tmux split-window -p 90 'zsh -ci e'"
# bind-key 0 run "tmux split-window; tmux send-keys 'fs' Enter"
# bind-key 0 run "tmux split-window; run-shell 'bash -ci fs'"

# bind-key 0 run "tmux split-window -l 12 'bash -ci ftpane'"

# new session
# bind-key n run "zsh -ci tmux"

# bind-key n run-shell "tmux new-session"

# In tmux.conf
# bind-key 0 run "tmux split-window -l 12 'bash -ci fs'"
#
#
#
#
# current working directory was removed, this restores it.
unbind c
# unbind '"'
# unbind %
bind c    new-window    -c '#{pane_current_path}'
# bind '"'  split-window  -c '#{pane_current_path}'
# bind %    split-window  -c '#{pane_current_path}' -h
#

## List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'tmux-plugins/tmux-open'
set -g @plugin 'tmux-plugins/tmux-sessionist'

# auto resurrect
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'
set -g @continuum-boot 'on'
set -g @continuum-boot-options 'iterm'
set -g @plugin 'tmux-plugins/tmux-cpu'
# CPU
# set -g status-right "CPU: #{cpu_icon} #{cpu_percentage} | %a %h-%d %H:%M "


# set -g @plugin "dweidner/tmux-theme"

# Other examples:
# set -g @plugin 'github_username/plugin_name'
# set -g @plugin 'git@github.com/user/plugin'
# set -g @plugin 'git@bitbucket.com/user/plugin'

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.tmux/plugins/tpm/tpm'

# set -g @resurrect-save 'S'
set -g @resurrect-restore 'R'
set -g @continuum-restore 'on'
