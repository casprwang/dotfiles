### For Installation 

in terminal

    touch .bash_profile
    vim .bash_profile

Then copy these codes in .bash_profile

    mkdir dotfiles 
    mv .vim dotfiles/ 
    mv .vimrc dotfiles/.vim
    -s ~/dotfiles/.vim ~/.vim 
    -s ~/dotfiles/.vim/.vimrc ~/.vimrc
