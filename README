wanngsong's dotfiles

for Installation
change the .bash_profile with:
$ mkdir dotfiles 
$ mv .vim dotfiles/ 
$ mv .vimrc dotfiles/.vim
$ -s ~/dotfiles/.vim ~/.vim 
$ -s ~/dotfiles/.vim/.vimrc ~/.vimrc

